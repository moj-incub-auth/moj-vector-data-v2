apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: hmrc-design-listener
spec:
  triggers:
    - bindings:
        - ref: git-repo-binding
      template:
        ref: hmrc-design-template