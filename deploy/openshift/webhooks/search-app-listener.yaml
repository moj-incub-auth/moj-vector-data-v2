apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: search-app-listener
spec:
  triggers:
    - bindings:
        - ref: git-repo-binding
      template:
        ref: search-app-template