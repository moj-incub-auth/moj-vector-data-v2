apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: ingestion-ai-app-listener
spec:
  triggers:
    - bindings:
        - ref: git-repo-binding
      template:
        ref: ingestion-ai-app-template