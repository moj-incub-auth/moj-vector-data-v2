apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: build-ingest-app-piperun-
spec:
  pipelineRef:
    name: build-ingest-app-pipeline
  serviceAccountName: pipeline 
  workspaces:
    - name: shared-data
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 40Gi